# Simpler Notes

Нативное десктоп приложение для заметок на Rust (gpui).

## Disclaimer

**Данное приложение написано при помощи ИИ агента** - Автор не принимал участие в написании кода, только ревьюил его и говорил, что и как переделать при необходимости.

**Автор не является Rust разработчиком** - До этого проекта и после него - автор не является Rust разработчиком - автор Go разработчик. Автор не имеет опыта в написании desktop приложений - данное приложение скорее эксперимент - смотрите код осознавая это

**Несмотря на то, что проект полностью написан ИИ, если у вас возникнет желание как-то прокомментировать код, можно завести issue или pr**

## Суть

Плоские Markdown-файлы на диске, один к одному с файловой системой.
Никакой базы данных — заметки это просто `.md` файлы в папке (vault).
Прикреплённые файлы (картинки) хранятся рядом с заметкой и видны в файловой системе.

### Ключевые фичи

- `@теги` — пользовательская организация вместо папок/канбанов/типов
- `[[вики-ссылки]]` — связи между заметками
- `!DD.MM.YYYY` — даты, которые автоматически появляются на таймлайне
- Timeline, Graph View, QL-поиск (гибрид индекса и ripgrep)

### Чем не является

- **Не task-трекер** — нет отдельной сущности «задача». Но можно трекать
  задачи через даты: пользователь сам строит эту абстракцию через `@теги`
  и `!даты`.
- **Не Notion/Obsidian** — никаких баз данных или проприетарных форматов.
- **Не AI-чат** — агент подключается через MCP, отдельно от интерфейса.

## Архитектура

Проект разделён на три крейта:

- `simpler-notes-core` — бизнес-логика: парсер, хранилище, индексы, git, watcher
- `simpler-notes-gui` — десктоп приложение на gpui
- `simpler-notes-mcp` — MCP-сервер для headless-доступа (агент, скрипты)

## Статус

Ранняя разработка. Полная спецификация: `docs/SPEC/README.md`

Концепт и хотелки: `DESIGN-DOCUMENT.md`

## TODO

- [x] 1 тест на 1 функцию — мало, нужно больше
- [x] относительные пути - мне не нравится. хочу просто название.
- [x] Возможно стоит оптимизировать парсинг. 
- [x] **резолв относительных путей в ссылках не нормализуется**. Нужно чинить `reindex_file` — резолвить target относительно source файла.
- [x] mod.rs мне не нравится. описания и все такое нужно положить к самим тулзам а их унифицировать - одна структура, разные хендлеры. 
- [x] Обдумать индексы и их структуру - нынешняя написана по наитию и не решает проблемы(разрешение пути перехода при клике на линк и тд)
- [x] пересмотреть индекс ссылок: очень неоднозначно, как оно строит путь. а то сейчас в мепе по линкам сохраняются откуда этот линк направлен на меня. но нет информации куда линк направлен.
- [ ] Однопроходный парсинг
- [ ] Пересчет индексов только при изменении файлов.
- [ ] Пересчет только индексов, затрагивающих измененный файл.
- [ ] Сделать пути в индексах относительными
